const suma = (a, b) => a + b;
const minus = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;

const matematika = {
    sudeti: suma,
    atimti: minus,
    sudauginti: multiply,
    dalinti: divide

}

export {
    suma,
    minus,
    multiply,
    divide
}